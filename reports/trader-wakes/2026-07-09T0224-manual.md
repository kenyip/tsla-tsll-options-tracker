# Trader wake — 2026-07-09T02:24-0700 — manual (continuation)

## Chose
Finish harden of evolve SHIP bar / scoring (`score_sim_metrics`) after NFLX thin-perfect + inf-PF polluted ranking; revalidate bad candidate hyps (paper only).

## Did
- Closed `score_sim_metrics` ship bar in `trader_platform/evolve_tick.py`:
  - `MIN_TRADES_SHIP=15`, `MIN_TRADES_SIGNAL=8`, `PF_CAP=10`, `MAX_ABS_SCORE=1e6`
  - non-finite PF → cap (never inf score)
  - thin perfect (n < 15, WR≥95%, DD≈0) → `NEEDS_MORE_DATA` / `thin_perfect_sample` with hard score cap
  - few trades (n < 8) → `NEEDS_MORE_DATA` with hard score cap
  - `VERDICT_RANK` sort so SHIP outranks NEEDS_MORE_DATA even if raw score lower
- Added pure offline `assert_ship_bar()` + wired into `trader_platform/smoke_test.py`
- Rescored all DNA `last_sim` in registry under ship_bar_v0.1 (0 non-finite scores remain)
- Engine re-sim of 3 NFLX DNAs (2y, cache) — all demoted off SHIP

## Evidence
- Offline: `assert_ship_bar()` PASS; `py_compile` evolve_tick + smoke_test OK
- Smoke: `python -m trader_platform.smoke_test` → platform smoke OK (+ evolve ship bar asserts OK)
- NFLX engine re-sim (no longer SHIP / no inf):
  - `hyp_dna_nflx_short_put_credit_5070a402` → NEEDS_MORE_DATA n=7 few_trades score=25 pf_cap=10
  - `hyp_dna_nflx_wheel_assignment_3783763c` → NEEDS_MORE_DATA n=10 thin_perfect_sample score=40
  - `hyp_dna_nflx_wheel_assignment_23f07585` → NEEDS_MORE_DATA n=8 thin_perfect_sample score=40
- Still SHIP under new bar (paper/testing only — not production SHIP, not shadow/live):
  - `hyp_dna_tsll_wheel_assignment_42870691` n=27 score≈388 (top among remaining)
  - `hyp_dna_tsll_short_put_credit_361f7bca` n=30 score≈180
  - `hyp_dna_tsll_wheel_assignment_cf2928dd` n=23 score≈156
  - `hyp_dna_smci_short_dte_aggressive_85932789` n=64 score≈114
  - `hyp_dna_tsll_roll_defend_969b22ab` n=44 score≈108
  - `hyp_dna_tsll_short_dte_aggressive_df7d51e0` n=39 score≈92
- Paths: `trader_platform/evolve_tick.py`, `trader_platform/smoke_test.py`, `trader_platform/data/hypotheses.yaml`, `.cache/platform/evolve_audit.jsonl`

## Durable changes
- Ship bar + ranking + finite scores in evolve_tick (library-pure scorer)
- Smoke regression for the historical NFLX inf-score failure mode
- Registry DNA last_sim rescored; NFLX hyps tagged `revalidate_required=ship_bar_v0.1` with null_results audit lines
- No shadow/live promotion; no live orders

## Next seed
Regime-stress the top remaining paper SHIP DNA (`hyp_dna_tsll_wheel_assignment_42870691` / siblings) with longer period or scenario windows before any paper→shadow packet; do **not** re-promote NFLX thin samples until n≥15 with losses present (finite PF from real losses). Optional: run `just evolve-tick` dry after ship bar to confirm ranking order SHIP ≫ thin NEEDS_MORE_DATA.

## Gates
none (paper-side only). Revalidation note for Ken: three NFLX candidates were promoted on polluted inf scores — now demoted to NEEDS_MORE_DATA/candidate. Six denser TSLL/SMCI hyps remain testing/SHIP under evolve bar only.

## Runtime
- trigger: manual continuation (interrupted self-evolution wake)
- loop: scoring harden + revalidate only
- model: grok-4.5 / trader profile
