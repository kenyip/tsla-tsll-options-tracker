# Trader MOA BUILD executor — 2026-07-13T1645

Status: `MOA_EXEC_DONE` — Phase 1 executor residue only. **RUN INCOMPLETE** until Grok challenge, Sol finalization, deterministic integration, push, and postflight receipt.

## Orientation and choice

- Phase/session: BUILD, postclose; inspected at `2026-07-13T16:56:31-0700` (`PDT`). Research/paper only on the isolated `$3,000` sleeve.
- Living leader: none. `reports/readiness/income-coverage-LATEST.md` explicitly retains the former TSLL PCS reference as failed. Absolute gates therefore govern; no historical candidate holds a seat.
- `orientation.json` carried 12 closed families, `redirect_required=false`, and left historical-underlying proxy discovery plus simulator-capability work executable while observed historical option replay remained blocked at only two TSLL market dates.
- Prior NEXT suggested one free defined-risk multi-structure discovery plus exact stress. I adopted the information-bearing part, but did **not** run an applied evolve. The loop was narrowed to dry-run discovery plus a reusable fail-closed pre-registration reject gate so weak full-sample `SHIP` labels cannot pollute the registry.
- Freedom audit: searched eight ranked symbols across bullish, bearish, neutral, and pin-oriented defined-risk structures; the choice was evidence/risk driven, not TSLA/TSLL or wheel constrained.

## Hypothesis and falsifier

Hypothesis: a dry-run evolve `SHIP` should be blocked from registry mutation unless it survives non-vacuous baseline, B3 regime, B4 5% slippage, fixed `$0.01` half-spread per leg, one-lot max-loss, window drawdown, and dense-negative-window gates. Because the evolve selection sample and five-year stress history can overlap, even a complete proxy pass must remain registration-ineligible until chronological train selection and an untouched holdout are proven.

Falsifier: reject the capability if it accepts an applied/unknown-provenance evolve report, lets a thin or non-positive cost axis pass, lets max loss above `$300`, window DD above `$75`, or dense-negative windows above five pass, or marks any overlapping proxy result registration-eligible.

## Actions and durable capability

1. Refreshed current research with `just research-tick-paper --sleeve-usd 3000` (`run_id=35` in `.cache/platform/research_runs/run_000035.json`). No live or broker path was used.
2. Ran one deterministic dry discovery (`seed=1645`, `period=2y`, population 36, top eight symbols, two mutants, max population 36) across `put_credit_spread`, `call_credit_spread`, `iron_condor`, and `butterfly_spread`. Source: `reports/trader-wakes/moa/2026-07-13T1645/evolve-discovery.json`.
3. Added `scripts/evolve_pre_registration_stress.py`, a research-only adapter that:
   - requires explicit `dry_run=true` and `applied=false` provenance;
   - deduplicates structure-pure single-symbol SHIPs without writing the hypothesis registry;
   - reuses the existing B3 regime, percentage-slippage, and fixed-dollar stress machinery;
   - records `capital_fit_usd`, one-lot `max_loss_usd`, and operating `max_lots=1`;
   - applies absolute non-vacuity/cost/risk/drawdown/dense-negative gates; and
   - hard-codes registration ineligibility while chronological untouched holdout proof is absent.
4. Added `tests/test_evolve_pre_registration_stress.py` with behavioral and fail-closed boundaries for unique SHIP loading, applied/missing/false dry-run provenance, a full proxy pass that still cannot register, fixed-cost failure, thin 5% cost evidence, and oversized max loss.
5. Generated `reports/trader-wakes/moa/2026-07-13T1645/evolve-pre-registration-stress.json` from the exact discovery report over `period=5y` Black-Scholes proxy history.

## Evidence and decision

The dry evolve report proves `applied=false`, `dry_run=true`, `n_population=36`, and eight symbols: TSLL, SMCI, PLTR, SNAP, NFLX, BAC, COIN, SOFI. It emitted three transient SHIPs:

| DNA | Structure | Discovery | Five-year stress outcome | Capital |
|---|---|---|---|---|
| `dna_01f091495791` | SNAP call credit spread | n65, score 34.858 | B3/base SHIP failed; 5% n268/`-$165.00`; fixed `$0.01` n224/`-$514.46`; window DD `$191.24`; dense negatives 12 | fit `$3,000`; one-lot max loss `$192.60`; max lots 1 |
| `dna_ba1a0d0e3ab5` | PLTR put credit spread | n28, score -72.729 | base/B3/fixed pass, but 5% n2/`+$25.70` is vacuous; window DD `$249.26` | fit `$3,000`; one-lot max loss `$195.78`; max lots 1 |
| `dna_94875b5aa4cd` | PLTR put credit spread | n33, score -204.294 | base/B3/fixed pass, but 5% n0/`$0.00` is vacuous; window DD `$339.57` | fit `$3,000`; one-lot max loss `$199.56`; max lots 1 |

Decision: `REJECT_ALL_DRY_RUN_SHIPS_BEFORE_REGISTRATION`; `complete_proxy_gate_ids=[]`; `registration_eligible_ids=[]`.

This is a useful falsification and capability delta, not an edge claim. Every result is Black-Scholes proxy L0 evidence; the five-year stress can overlap the two-year selection sample; option-contract availability and observed fills are not proven. Capital fit alone does not overcome after-cost non-vacuity or loss quality. No hypothesis, leader, readiness B-check, paper order, shadow status, arm state, or live state changed.

## Verification

- Focused TDD: `python -m unittest tests.test_evolve_pre_registration_stress -v` → `6/6` green. The missing/false `dry_run` boundary failed before implementation, then passed after the fail-closed repair.
- Artifact rerun: three candidates, zero complete proxy gates, zero registration-eligible IDs, reject-all decision.
- Platform smoke: green; agentic-live remained blocked.
- Full regression: `python -m unittest discover -s tests -v` → `207/207` green.
- Syntax/hygiene: `py_compile` green; `git diff --check` green.
- Targeted security scan: no hard-coded secret, shell injection, dangerous eval/exec, pickle load, or SQL f-string pattern in the two new files.
- Coverage refreshed with `scripts/trader_income_coverage.py --write`, canonicalized to the wake stamp, and identical between latest/dated surfaces: 21 structures, 245 hypotheses, 70 evolve artifacts, no living leader.
- `reports/readiness/LATEST.md` was intentionally not changed because phase, leader, and B-check status did not change.

## Durable lesson

A raw evolve `SHIP` is only a discovery label. Before any registry write, Trader now has a reproducible reject gate that separates baseline score from B3/B4/fixed-cost/risk/drawdown quality and fails closed on unproven dry-run provenance. Even a complete overlapping proxy pass remains ineligible without chronological untouched holdout evidence.

## NEXT

Build one chronological pre-registration adapter that performs train-only evolve selection and then evaluates the selected DNA on an untouched holdout with the same B3, 5% slippage, fixed `$0.01` per-leg, max-loss, window-DD, and dense-negative gates; run exactly one fixed-seed free defined-risk multi-structure cycle, keep L0, and register nothing on the first pass.

Safety: research/paper only; no broker login, live order, spend, shadow/live promotion, agentic arm, commit, push, or merge occurred in this executor phase.
