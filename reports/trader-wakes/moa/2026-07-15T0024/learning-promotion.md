# Learning promotion — 2026-07-15T0024

Strategy charter: a path-dependent five-session TSLL tracking shortfall versus twice TSLA's five-session return was tested as a transient leveraged-ETF rebalance/noise dislocation under a positive fully warmed TSLA trend and a five-session TSLA return floor of -5%. The frozen scope was `TSLL_RELATIVE_DISLOCATION_BULL_CALL_14D_V1` / `TSLL_TSLA_5D_TRACKING_SHORTFALL_REBOUND`: residual `r_TSLL(5d)-2*r_TSLA(5d) <= -4%`, next-session entry, five-session hold, non-overlapping treated episodes, and earlier neutral-residual controls in `[-1%,+1%]` matched without replacement inside fixed TSLA-return, trend-distance, calendar-distance, and disjoint-window bounds. A future 14-DTE `$1`-wide bull-call debit spread was structural context only (`capital_fit_usd=100`, one-lot `max_loss_usd=100` width bound before closing friction, `max_lots=1`).

Outcome: `FAMILY_CLOSED`; strategy advancement false; `F0_MECHANISM -> F0_MECHANISM`. The 984-row adjusted TSLA/TSLL common panel produced 454 eligible dates, 36 treated shortfall candidates, and 274 neutral-residual control candidates, but zero matched blueprints under the complete frozen prior-only geometry. Train `n_pairs=0` failed the predeclared minimum 20 before return estimation. Treated/control return, paired-excess, and bootstrap fields remain strict JSON `null`; holdout outcomes remain unread; `pricing_calls=0`. Dominant failure is control support, not positive or negative rebound expectancy. The exact family and unchanged residual/threshold/match-bound/horizon variants are quarantined; no wider post-hoc matching, holdout peek, option rescue, L1, capital seat, registry, paper, shadow, funding, broker, arm, or live authority exists.

Accepted challenger findings: preserve the close/no-rescue boundary; treat residual p05 approximately -4.865% and the 36-event tail as design-feasibility context only; do not reinterpret null metrics as signed no-edge; rerun finalizer verification independently; repair living readiness to active epoch `2026-07-15-viable-path`; record active-epoch no-advance streak one without pivot/burst-stop; tighten the cadence-policy NEXT around a frozen signal stream and explicit policy knobs. Finalizer also repaired a durable-evidence gap not called out by the challenger: the claim artifact is now tracked under the run directory rather than existing only in ignored cache.

Rejected interpretations: post-hoc widening of residual, covariate, calendar, or horizon bounds is not a valid rescue; the thin tail is not rebound evidence; null metrics do not estimate expectancy; structural `$100` width is not observed max loss or a seat; executor self-reported test counts are not finalizer verification; prior-epoch burst-stop is not the living action; holdout inspection, option pricing, nearby retuning, or paper/shadow/arm/live action cannot close this run.

## VERIFICATION

- `.venv/bin/python -m unittest -v tests.test_tsll_tracking_dislocation_train_lab tests.test_low_hv_cross_section_train_lab` -> final rerun `Ran 14 tests in 0.395s`, `OK`. Behavioral/boundary/negative controls cover prior-completed selection, prior-only/no-reuse/disjoint controls, entry-bar shock invariance, positive-gate behavior, positive-point/non-positive-bootstrap rejection, unread holdout, no option pricing, strict structural capital labels, direct CLI import, zero-treated vacuity, and the exact treated+neutral-but-zero-frozen-support failure path.
- `.venv/bin/python -m unittest -v tests.test_trader_completion_contract` -> `Ran 12 tests in 0.671s`, `OK`; smart stranded-run recovery and zero-input completion surfaces remain green.
- `.venv/bin/python -m py_compile scripts/tsll_tracking_dislocation_train_lab.py tests/test_tsll_tracking_dislocation_train_lab.py` -> exit `0`.
- `.venv/bin/python scripts/tsll_tracking_dislocation_train_lab.py --out reports/trader-wakes/moa/2026-07-15T0024/tsll-tracking-dislocation-train.json` plus unchanged-source rerun to `/tmp/tsll-tracking-dislocation-train-finalizer.json` -> exit `0`; `TRACKED_SUBSTANTIVE_REPRODUCTION_OK` after excluding only `generated_at`; `FAMILY_CLOSED`, 454 eligible / 36 treated / 274 neutral / 0 matched, train n0, holdout unread, pricing calls 0; final tracked SHA-256 `863aeec035c624ff5117cb3c37227d9c0286449ee5f58ef537565e06ea4f3386`.
- `.venv/bin/python scripts/trader_income_coverage.py --stamp 2026-07-15T0024` plus dated/LATEST `cmp` -> exit `0`, `COVERAGE_SURFACES_IDENTICAL`; 21 structures, 246 hypotheses, 70 evolve artifacts, living leader none.
- `just platform-smoke` -> exit `0`, `platform smoke OK`; `agentic_live` remained blocked at the Stage1 OAuth gate.
- `.venv/bin/python -m unittest discover -s tests` -> final rerun `Ran 289 tests in 13.238s`, `OK`.
- `.venv/bin/python scripts/trader_build_compounding.py validate-handoff --repo . --stamp 2026-07-15T0024 --base-head 598710a9c37ac4c8eccfbb186993954c41c7e4f5 --baseline .cache/platform/completion/2026-07-15T0024-finalizer-baseline.json` -> exit `0`; `ok=true`, `role_ready=true`, schema `2`, outcome `FAMILY_CLOSED`, strategy advancement false, four useful deltas, seven critic findings closed.
- Temporary-index deterministic wrapper rehearsal: staged all 25 intended files in an isolated index and ran `.venv/bin/python scripts/trader_run_completion_gate.py prepare --repo . --stamp 2026-07-15T0024 --base-head 598710a9c37ac4c8eccfbb186993954c41c7e4f5 --run-branch trader/run-2026-07-15T0024` -> exit `0`, `ok=true`, branch/stamp/base correct. The first rehearsal correctly failed on a raw `executor-session.prev.log` plus trailing whitespace in the strategy charter; the run-created raw session log was removed and the charter whitespace repaired before the green rerun. Complete staged diff scan found no private key, AWS key, GitHub token, generic added secret assignment, private-position YAML, or raw session log. The real Git index SHA-256 was identical before/after (`REAL_INDEX_UNCHANGED`).
- `git diff --check 598710a9c37ac4c8eccfbb186993954c41c7e4f5` -> exit `0`.

## DURABLE

Every material challenger finding is reconciled:

1. `FAMILY_CLOSED`, F0→F0, and no strategy advancement are aligned across the tracked canonical JSON, final merge/LATEST/INDEX, readiness, and schema-v2 handoff. No expectancy is invented from zero pairs.
2. `tests/test_tsll_tracking_dislocation_train_lab.py` now includes the exact dominant-failure negative control: treated and neutral populations both exist, frozen support is zero, and the lab must close with null expectancy, unread holdout, zero pricing calls, and the control-support label. The reusable machinery remains `scripts/tsll_tracking_dislocation_train_lab.py`.
3. Finalizer verification is independent: focused/shared 14/14, completion-contract 12/12, compile, exact tracked experiment and substantive reproduction, coverage, platform smoke, and full 289/289 are green.
4. The canonical claim artifact was promoted from ignored `.cache` into `reports/trader-wakes/moa/2026-07-15T0024/tsll-tracking-dislocation-train.json`; earlier executor/challenger SHA `6206bdf1…` belongs to their cache-phase regeneration, while the final tracked payload is substantively identical excluding only `generated_at` and is cited by all living finalizer surfaces.
5. `reports/readiness/LATEST.md` now names active epoch `2026-07-15-viable-path`, this close as no-advance streak one, pivot/burst-stop false, living leader none, empty capital path, and the merged cadence-policy NEXT. Prior epoch `2026-07-14-reassess` remains historical only.
6. Redundant run-created coverage snapshots at 0200/0515/0613/0621 were removed; the canonical wake-stamped `income-coverage-2026-07-15T0024.md` is byte-identical to LATEST.
7. The profile-local `trader-self-evolution` skill now records the reusable control-support pitfall: zero frozen pairs despite treated/control populations is design-feasibility failure, not signed expectancy; metrics stay null, holdout/options stay untouched, exact negative control is required, and inspected bounds cannot be widened post hoc.

Dated outcome/current project truth is promoted through tracked run evidence, final wake/LATEST/INDEX, readiness, coverage, and `compounding.json`. Trader profile memory is unchanged because this is dated project evidence plus a reusable procedure, not a compact stable Ken preference or routing fact. The existing stranded-recovery machinery/doc/test delta is preserved as intended run work and remains green; the separate 0631 RTH report is preserved rather than absorbed or deleted.

Integration is pending the deterministic wrapper gate. The finalizer did not stage the real Git index, commit, push, merge, switch branches, edit `.gitignore`, place an order, or claim completion.

## LESSON

Future Trader can distinguish a selector with no admissible counterfactual support from a selector with measured negative expectancy. When treated and neutral populations exist but the frozen prior-only match geometry yields zero pairs, close the exact design at F0, serialize unavailable return/bootstrap metrics as strict JSON `null`, preserve unread holdout and no-option boundaries, and quarantine unchanged bounds. A materially new predeclared control design may be researched later, but widening inspected tolerances is not evidence. Future Trader can also persist claim-bearing output directly under the tracked MoA run directory so deterministic integration preserves the evidence instead of relying on ignored cache.

## NEXT

`ONE_RISK_UNIT_CADENCE_POLICY_F0`: in the next zero-input BUILD wake, freeze one concrete chronological defined-risk signal stream with named structures, symbols, DNA provenance, and no closed-family retunes; predeclare one-open-risk-unit, correlation/symbol-cluster cap, and concurrency rules; compare the capped policy with the identical uncapped stream on train only under labeled dual-cost/path-realism semantics; require non-vacuous drawdown improvement and non-negative expectancy; reserve untouched holdout unless train passes; discovery bar only, with no registry auto-promote, paper force, shadow, arm, broker, or live action.
