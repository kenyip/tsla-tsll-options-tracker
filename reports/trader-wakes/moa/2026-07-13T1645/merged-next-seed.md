# Merged NEXT seed — 2026-07-13T1645 (challenger)

Prior free multi-structure dry discovery + pre-registration absolute-gate falsify is **done** this wake: three transient SHIPs rejected; complete proxy gates 0; registration eligible 0; reusable dry-run pre-registration stress gate landed. Do not re-run the same full-sample dry pop without the chronological split.

**ONE NEXT:**

Build one chronological pre-registration adapter that:
1. Performs **train-only** evolve selection (fixed seed, free multi-structure defined-risk, top research symbols, sleeve $3000), and
2. Evaluates every selected DNA on an **untouched chronological holdout** with the same absolute gates already encoded in `scripts/evolve_pre_registration_stress.py`:
   - baseline non-vacuous positive SHIP
   - B3 regime soft-hold
   - 5% slip non-vacuous positive SHIP
   - fixed `$0.01` per-leg non-vacuous positive SHIP
   - one-lot `max_loss_usd` ≤ `$300`
   - window max DD ≤ `$75`
   - dense-negative windows (n≥3) ≤ 5
3. Keeps `registration_eligible=false` unless train **and** holdout complete gates pass (conjunctive).
4. Runs **exactly one** fixed-seed free defined-risk multi-structure L0 cycle.
5. Registers **nothing** on the first pass; labels Black-Scholes proxy / L0 only.

Hard redirects:
- Do **not** reopen closed families (session-time densified seed, double-diagonal 14/60, collar, asymmetric IC, daily-bar signal families, BAC Fri7 management, AAPL ex-date inventory).
- Do **not** applied-evolve / registry-write from overlapping full-sample SHIP alone.
- Do **not** claim provider-backed historical option edge or L1 while TSLL observed archive remains &lt;3/3 market dates.
- Optional finalizer-only soft repair: derive `capital_fit_usd` from observed one-lot max loss rather than hard-coding 3000; keep operating `max_lots=1`.
- RTH parallel only: next **distinct** NY market date may append all-expiration TSLL archive 2→3/3 without same-date thrash; three dates are plumbing/calibration floor, not edge.

Outcome class expected: capability + honest falsification / or rare train∧holdout complete proxy pass still L0 research-only — not full-sample SHIP thrash, not paper/shadow/arm/live.
