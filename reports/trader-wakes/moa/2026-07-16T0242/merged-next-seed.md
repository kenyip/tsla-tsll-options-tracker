# Merged NEXT seed — 2026-07-16T0242

Status: challenger-accepted seed for finalizer / next zero-input orientation
Authority: research/BUILD only; no paper force, shadow, arm, broker, funding, or live

## Exactly one NEXT

`MACRO_INFORMATION_RESOLUTION_TWO_CLOSE_PIVOT_REASSESSMENT`

## Why this seed (not CPI-by-default)

Two independent scheduled-macro F0 families are now closed without advancement:

1. `FOMC_INFORMATION_RESOLUTION_SPY_BULL_CALL_21D_V1` / `FOMC_POLICY_INFORMATION_RESOLUTION_DRIFT` — signed bullish five-session SPY drift failed absolute/paired/frequency/LB90 gates after control-coverage repair.
2. `BEIGE_BOOK_RANGE_COMPRESSION_SPY_IC_21D_V1` / `BEIGE_BOOK_INFORMATION_RESOLUTION_RANGE_COMPRESSION` — range compression vs prior-only neutral controls failed the frozen `>=55%` positive-frequency consistency gate (49.09%) despite thin positive mean and barely positive year-block LB90.

Prior readiness NEXT `CPI_RELEASE_INFORMATION_RESOLUTION_SPY_F0` is **adjacency context only**, not an order. This wake already found BLS/CPI official retrieval blocked (HTTP 403 / web-search backend unconfigured) from the current environment. Do not thrash that blocked path or treat access failure as proof that CPI economics are invalid.

## Decision the next wake must close

Choose **exactly one**:

A. **Open a genuinely different CPI/inflation-surprise route** only if official prior-known source identity, coverage start, exact or fail-closed timestamp semantics, candidate vs control-exclusion scopes, and surprise/information content are frozen **before outcomes** and are materially different from both closed FOMC direction and Beige Book range mechanisms; or

B. **Pivot the search design away from scheduled macro information-resolution** to a materially different economic mechanism/evidence class (non-quarantined), with a complete Layered Edge Stack and predeclared falsifier.

Do **not**:

- inspect either sealed holdout (FOMC 32; Beige Book 38);
- retune either closed family’s thresholds, horizons, regimes, controls, signs, or option wrappers;
- force an option expression onto a failed underlying screen;
- open a new epoch by silently extending the completed FOMC epoch record;
- claim L1/seat/paper/shadow/arm/live.

## Required freeze if CPI is chosen

Before any outcome read:

- official source identity + coverage start;
- separate candidate-event scope from control-exclusion scope;
- forbid pre-coverage controls;
- ambiguous/missing time → stand-aside or next-session-only design with labeled assumptions;
- prior-only same-regime no-reuse controls;
- chronological train + sealed unread holdout;
- dependence-aware uncertainty / density / support / tail gates;
- zero option marks at F0;
- future structure, if any, stated only as planning capital (`capital_fit_usd`, one-lot `max_loss_usd`, `max_lots=1`).

## Required freeze if non-macro pivot is chosen

Name a new economic mechanism not covered by closed-family IDs/aliases, complete the Layered Edge Stack, predeclare the falsifier and evidence class, and close one of: `STRATEGY_ADVANCED` | `FAMILY_CLOSED` | `BLOCKER_REMOVED_AND_RETESTED` | `EVIDENCE_WAIT`.

## Closed IDs that must remain quarantined

- `FOMC_INFORMATION_RESOLUTION_SPY_BULL_CALL_21D_V1`
- `FOMC_POLICY_INFORMATION_RESOLUTION_DRIFT`
- `BEIGE_BOOK_RANGE_COMPRESSION_SPY_IC_21D_V1`
- `BEIGE_BOOK_INFORMATION_RESOLUTION_RANGE_COMPRESSION`
