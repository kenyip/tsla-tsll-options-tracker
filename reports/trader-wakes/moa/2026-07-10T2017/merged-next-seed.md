# Merged next seed — 2026-07-10T2017

Next BUILD wake only (not RTH evolve): falsify transient TSLL PCS time variant `time_variant_tsll_pcs_dte7_pt35_stop5` (7 DTE / 35% target / dte_stop 5; unregistered) for **non-vacuous after-cost income**.

1. Optionally add **one** entry-weekday (or session) filter to pcs_sim + `pcs_time_bias_grid` if required to slice the check — that is the remaining time-axis coverage gap, not a promotion path.
2. Run a dated 5% slip cost stress (and a thin weekday/OOS slice if filter was added).
3. **Pass condition to keep researching the variant:** n_trades ≥ 1 at 5% slip with defined-risk ml still competitive vs `b195f5fe`.
   **Success-to-stop condition:** still n_trades = 0 at 5% slip → **reject the variant as research-dead** and end the wake (do not register, do not open a second engineering loop on it).
4. Leave capital path and B6 target on registered leader `hyp_dna_tsll_put_credit_spread_b195f5fe` unless a non-vacuous after-cost beat appears (it has not).

Constraints: paper/research only; no evolve capital-path promote; no shadow/live; no agentic arm; no multi-item menu.
