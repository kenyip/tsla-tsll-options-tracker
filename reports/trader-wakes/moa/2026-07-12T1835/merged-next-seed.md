# Merged NEXT seed — 2026-07-12T1835

## ONE NEXT

Cross-check archived Nasdaq cash-dividend events for **AAPL** (and **MU only if a normalized archive is captured first**) against one independent issuer/filing announcement source. Add a per-symbol provenance qualification **only if** event completeness and security-class identity can be proved. Otherwise keep the Nasdaq provider **L0 / single-source**, do not invent multi-source confidence, and redirect the next zero-input wake to another open historical-underlying or simulator-capability route.

## Constraints

- Paper/research only; no live, shadow auto-promote, broker login, spend, or agentic arm.
- Do not backfill `known_at` from ex-date, record date, payment date, or capture time.
- Empty/ambiguous Nasdaq history remains `None` coverage, never covered/no-event `[]`.
- Provider does not unlock L1; split-unadjusted amount scale, exact security class, Black-Scholes marks, and missing observed option surfaces still block calibrated assignment/edge claims.
- Supported-listing required mode is capability plumbing, not a capital seat.
- Coverage begins at the earliest retained `known_at`; preserve the first-event pre-ex service and post-observation tamper controls.
- Zero-input next wake may supersede this seed when the cross-check is blocked/slow and a different open route is higher-information — state why.
- Parallel only on a distinct NY RTH date: TSLL all-expiration archive append 1→2/3 (orientation data dependency, not this loop’s seat).

## Closed this wake (context, not allowlist)

Archived Nasdaq declaration-date `DividendEventProvider` for supported eventful listings with fail-closed unsupported/ambiguous paths, AAPL live archive + required-mode diagonal/bull-call wiring smoke, and explicit claim boundary that this is corporate-action guard input only. Prior rejected daily-bar signal families and empty capital path remain unchanged.
